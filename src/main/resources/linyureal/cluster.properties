# Overrides for jobs submitted inside existing Flink Docker containers.
kafka.bootstrap.servers=kafka:9092
doris.fenodes=doris-fe:8030
# Mount the SAME durable host directory on JobManager and all TaskManagers.
checkpoint.directory=file:///opt/flink/checkpoints/linyureal
